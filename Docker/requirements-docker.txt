# ═══════════════════════════════════════════════════════════════
# PortDesk — Docker Requirements (Base + Docker Runtime)
# ═══════════════════════════════════════════════════════════════

# ── Screen Capture & Streaming (Required) ────────────────────
opencv-python-headless==4.10.0.84
numpy==1.26.4
mss==9.0.2

# ── WebRTC (Required — for peer-to-peer streaming) ──────────
aiortc==1.9.0
av==12.3.0
aioice==0.9.0

# ── SSL Certificate Generation (Required) ────────────────────
cryptography==42.0.8

# ── Platform-Optional (Auto-installed by SERVER.py on first run) ──
# These packages are automatically installed by _auto_install_deps()
# in SERVER.py if missing.  They are listed here so the Docker image
# bakes them in at build time instead of installing at container start
# (faster startup, no pip-at-runtime, deterministic image).
#
# Linux:
python-xlib==0.33       # Fast X11 input control via XTest extension
evdev==1.7.1            # Kernel-level input for Wayland/headless
uvloop==0.21.0          # 2-4x faster event loop (libuv-based)
#
# To disable auto-install on non-Docker runs:
#   export PORTDESK_NO_AUTOINSTALL=1

# ── Auto-installed by SERVER.py (also baked into Docker image) ──
# PyTurboJPEG: hardware-accelerated JPEG encoding via libturbojpeg.
# SERVER.py's _auto_install_deps() installs this on first run if missing.
# Listed here so Docker bakes it in at build time (faster startup).
# The server falls back to cv2 JPEG encoding if PyTurboJPEG or
# libturbojpeg is not available.  If you don't want it:
#   1. Comment out PyTurboJPEG below
#   2. Remove libturbojpeg0-dev from Dockerfile apt-get line
PyTurboJPEG==1.6.3
